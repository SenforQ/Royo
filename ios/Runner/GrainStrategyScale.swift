
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let userIndexData:String = "getDevclick point ok point app"
fileprivate let showObserverContactValue:[Character] = ["i","c","e","I","D"]

/*: "getFirebaseID" :*/
fileprivate let appLayerId:String = "getFirebawhite temp success can"
fileprivate let showStatePath:[Character] = ["s","e","I","D"]

/*: "getAreaISO" :*/
fileprivate let user_messageStr:String = "getArinterval for i input appear"
fileprivate let user_reportKey:String = "fail permission vertical openeaISO"

/*: "getProxyStatus" :*/
fileprivate let data_createName:String = "tillet"
fileprivate let constTransformKey:String = "Proxclose go receive"

/*: "getMicStatus" :*/
fileprivate let k_indexStr:[Character] = ["g","e","t","M","i","c","S","t","a"]
fileprivate let appPhoneFormat:String = "tuscale"

/*: "getPhotoStatus" :*/
fileprivate let user_verticalValue:String = "tab mediagetP"
fileprivate let const_micPath:[Character] = ["a","t","u","s"]

/*: "getCameraStatus" :*/
fileprivate let user_carrierContent:String = "content earngetCam"
fileprivate let dataColorMsg:[Character] = ["a","t","u","s"]

/*: "reportAdjust" :*/
fileprivate let showHandFormat:String = "reselectr"

/*: "requestLocalPush" :*/
fileprivate let showClearStopId:[Character] = ["r","e","q","u","e","s","t","L","o","c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let app_usPath:String = "geempty"
fileprivate let userSessionDisplayTicketTitle:String = "close behavior trueLangCode"

/*: "getTimeZone" :*/
fileprivate let main_textPath:[Character] = ["g","e","t","T","i","m","e"]
fileprivate let noti_presentationId:String = "Zonetotal info"

/*: "getInstalledApps" :*/
fileprivate let mainIndexTitle:String = "select interval ofgetIns"
fileprivate let user_imageStr:String = "padd"

/*: "getSystemUUID" :*/
fileprivate let const_panelStr:[Character] = ["g","e","t","S","y","s","t","e","m","U","U","I","D"]

/*: "getCountryCode" :*/
fileprivate let app_stateTakeTitle:String = "fire"
fileprivate let const_sToolFinishFormat:String = "etCounfilter display break or"

/*: "inAppRating" :*/
fileprivate let kAlreadyData:[Character] = ["i","n","A","p","p","R","a","t","i"]
fileprivate let userFinishPath:[Character] = ["n","g"]

/*: "apPay" :*/
fileprivate let kDomainPadMsg:[Character] = ["a","p","P","a","y"]

/*: "subscribe" :*/
fileprivate let notiRemoteStr:String = "SUBS"

/*: "openSystemBrowser" :*/
fileprivate let user_keyFormat:String = "visible valueopenS"
fileprivate let notiTrustFormat:String = "mBrowsename decide now in"
fileprivate let mainBuildPath:[Character] = ["r"]

/*: "closeWebview" :*/
fileprivate let showActivityMessage:String = "handle space by start tempcloseW"
fileprivate let mainFireFormat:[Character] = ["e","b","v","i","e","w"]

/*: "openNewWebview" :*/
fileprivate let notiChallengeFormat:String = "openNewdocument where any transform show"
fileprivate let data_forgetNeverNotContent:String = "warn copy product list currencyWebview"

/*: "reloadWebview" :*/
fileprivate let notiFeedbackFailureKey:String = "granteloa"
fileprivate let showTitleStr:String = "globaliew"

/*: "typeName" :*/
fileprivate let app_fullMsg:[UInt8] = [0x65,0x6d,0x61,0x4e,0x65,0x70,0x79,0x74]

/*: "deviceID" :*/
fileprivate let appExecuteMakeMsg:[UInt8] = [0x44,0x49,0x65,0x63,0x69,0x76,0x65,0x64]

/*: "fireBaseID" :*/
fileprivate let k_afterName:[UInt8] = [0x67,0x6a,0x73,0x66,0x43,0x62,0x74,0x66,0x4a,0x45]

fileprivate func globalName(content num: UInt8) -> UInt8 {
    let value = Int(num) - 1
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "areaISO" :*/
fileprivate let data_intervalName:[UInt8] = [0xd7,0xc4,0xd3,0xd7,0xff,0xe5,0xf9]

private func alreadyLaterScheme(ok num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "isProxy" :*/
fileprivate let noti_usStr:[UInt8] = [0x23,0x39,0x1a,0x38,0x25,0x32,0x33]

private func magnitudeerrelateFrame(your num: UInt8) -> UInt8 {
    return num ^ 74
}

/*: "langCode" :*/
fileprivate let noti_photoWithId:[UInt8] = [0x65,0x64,0x6f,0x43,0x67,0x6e,0x61,0x6c]

/*: "timeZone" :*/
fileprivate let show_futureMsg:[UInt8] = [0x92,0x8f,0x8b,0x83,0xbc,0x89,0x88,0x83]

private func presentationUs(no num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "installedApps" :*/
fileprivate let app_managerOriginalTitle:[UInt8] = [0x63,0x68,0x6d,0x6e,0x5b,0x66,0x66,0x5f,0x5e,0x3b,0x6a,0x6a,0x6d]

fileprivate func launchFail(value num: UInt8) -> UInt8 {
    let value = Int(num) - 250
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "systemUUID" :*/
fileprivate let userApplicationId:[UInt8] = [0x2e,0x24,0x2e,0x29,0x38,0x30,0x8,0x8,0x14,0x19]

private func largerBlack(transform num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "countryCode" :*/
fileprivate let k_languageMessage:[UInt8] = [0x5,0x9,0x13,0x8,0x12,0x14,0x1f,0x25,0x9,0x2,0x3]

private func countBounce(photo num: UInt8) -> UInt8 {
    return num ^ 102
}

/*: "status" :*/
fileprivate let notiRefreshAccessFormat:[UInt8] = [0x85,0x86,0x73,0x86,0x87,0x85]

fileprivate func cameraLevel(remove num: UInt8) -> UInt8 {
    let value = Int(num) - 18
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isAuth" :*/
fileprivate let notiSinceMessage:[UInt8] = [0xc8,0xd2,0xa0,0xd4,0xd3,0xc7]

fileprivate func platformDevice(system num: UInt8) -> UInt8 {
    let value = Int(num) + 161
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isFirst" :*/
fileprivate let appFailureApplicationStr:[UInt8] = [0x1b,0x1,0x34,0x1b,0x0,0x1,0x6]

/*: __LocalPush" :*/
fileprivate let noti_statusMsg:String = "photo present open need all__Lo"

/*: "identifier" :*/
fileprivate let noti_indexData:[UInt8] = [0x73,0x7e,0x7f,0x74,0x6e,0x73,0x7c,0x73,0x7f,0x68]

private func forwardBridge(by num: UInt8) -> UInt8 {
    return num ^ 26
}

/*: "HTTPProxy" :*/
fileprivate let constPageMsg:String = "HTTPPrglobal load path version"
fileprivate let dataVisiblePath:String = "osystem"

/*: "HTTPSProxy" :*/
fileprivate let kPhotoMessage:[Character] = ["H","T","T","P","S","P","r","o"]
fileprivate let userAreaName:String = "emptyy"

/*: "SOCKSProxy" :*/
fileprivate let k_appearText:String = "SOCKSProxcomponent class"
fileprivate let mainMirrorPath:String = "environment"

/*: "__SCOPED__" :*/
fileprivate let k_dateTitle:String = "__"
fileprivate let show_mapAppId:String = "SadjustmentOP"

/*: "tap" :*/
fileprivate let const_nowValue:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let mainScreenValue:String = "tuspace"

/*: "ipsec" :*/
fileprivate let mainArrowPath:String = "ipfailec"

/*: "ppp" :*/
fileprivate let const_dismissName:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let userPageMsg:String = "display minimum feedbackRetry "
fileprivate let dataNowPath:[Character] = ["o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let dataNeverUrl:[Character] = ["F","e","e","d","b","a"]
fileprivate let app_trackName:String = "cview"

/*: " to contact us" :*/
fileprivate let notiLocalSuccessMsg:String = "active"
fileprivate let mainApplicationFormatMaskMsg:[Character] = ["t","o"," ","c","o","n","t","a","c","t"," ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GrainStrategyScale.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let main_onceZoneId = (String(userIndexData.prefix(6)) + String(showObserverContactValue)) // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let app_viewPath = (String(appLayerId.prefix(9)) + String(showStatePath)) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let constCenterPackageValue = (String(user_messageStr.prefix(5)) + String(user_reportKey.suffix(5))) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let data_methodId = (data_createName.replacingOccurrences(of: "till", with: "g") + String(constTransformKey.prefix(4)) + "yStatus") // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let mainEndScaleKey = (String(k_indexStr) + appPhoneFormat.replacingOccurrences(of: "scale", with: "s")) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let appFuturePath = (String(user_verticalValue.suffix(4)) + "hotoSt" + String(const_micPath)) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let mainKeyFormat = (String(user_carrierContent.suffix(6)) + "eraSt" + String(dataColorMsg)) // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let data_progressUrl = (showHandFormat.replacingOccurrences(of: "select", with: "po") + "tAdjust") // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let noti_backgroundMsg = (String(showClearStopId)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let noti_barTitle = (app_usPath.replacingOccurrences(of: "empty", with: "t") + String(userSessionDisplayTicketTitle.suffix(8))) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let main_localPath = (String(main_textPath) + String(noti_presentationId.prefix(4))) // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let showVersionTitle = (String(mainIndexTitle.suffix(6)) + "talledAp" + user_imageStr.replacingOccurrences(of: "add", with: "s")) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let data_mediaValue = (String(const_panelStr)) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let mainLevelMessage = (app_stateTakeTitle.replacingOccurrences(of: "fire", with: "g") + String(const_sToolFinishFormat.prefix(6)) + "tryCode") // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let main_productionName = (String(kAlreadyData) + String(userFinishPath)) // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let k_countryForgetMessage = (String(kDomainPadMsg)) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let const_alreadyMessage = (notiRemoteStr.lowercased() + "cribe") // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let notiTrustContent = (String(user_keyFormat.suffix(5)) + "yste" + String(notiTrustFormat.prefix(7)) + String(mainBuildPath)) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let app_persistName = (String(showActivityMessage.suffix(6)) + String(mainFireFormat)) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let showCycleMessage = (String(notiChallengeFormat.prefix(7)) + String(data_forgetNeverNotContent.suffix(7))) // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let mainMethodDecisionKey = (notiFeedbackFailureKey.replacingOccurrences(of: "grant", with: "r") + "dWeb" + showTitleStr.replacingOccurrences(of: "global", with: "v")) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct InformationProcessingSystemTransformable: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: WithoutUtiliserModelType?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct WithoutUtiliserModelType: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension ContainerNavigationDelegate {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func handle(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = InformationProcessingSystemTransformable.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case main_onceZoneId:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = PossibleLocalizationBrightness.earnAdid()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: appExecuteMakeMsg.reversed(), encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case app_viewPath:
                //: AppWebViewController.getFireBaseToken { str in
                ContainerNavigationDelegate.center { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: k_afterName.map{globalName(content: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case constCenterPackageValue:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = ContainerNavigationDelegate.camera()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: data_intervalName.map{alreadyLaterScheme(ok: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case data_methodId:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = ContainerNavigationDelegate.proxyStatus()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: noti_usStr.map{magnitudeerrelateFrame(your: $0)}, encoding: .utf8)!: status])

            //: case getLangCode:
            case noti_barTitle:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: noti_photoWithId.reversed(), encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case main_localPath:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: show_futureMsg.map{presentationUs(no: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case showVersionTitle:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: app_managerOriginalTitle.map{launchFail(value: $0)}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case data_mediaValue:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: userApplicationId.map{largerBlack(transform: $0)}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case mainLevelMessage:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: k_languageMessage.map{countBounce(photo: $0)}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case main_productionName:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                ContainerNavigationDelegate.file()

            //: case apPay:
            case k_countryForgetMessage:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.clearDeadline(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: notiRefreshAccessFormat.map{cameraLevel(remove: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case const_alreadyMessage:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.clearDeadline(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: notiRefreshAccessFormat.map{cameraLevel(remove: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case notiTrustContent:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case app_persistName:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.color()

            //: case openNewWebview:
            case showCycleMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    ContainerNavigationDelegate.outsideFrom(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case mainMethodDecisionKey:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.eraseInput()

            //: case getMicStatus:
            case mainEndScaleKey:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                RecoverPermission.shared.indicatorBlock { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: notiSinceMessage.map{platformDevice(system: $0)}, encoding: .utf8)!: auth, String(bytes: appFailureApplicationStr.map{$0^114}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case appFuturePath:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                RecoverPermission.shared.of { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: notiSinceMessage.map{platformDevice(system: $0)}, encoding: .utf8)!: auth, String(bytes: appFailureApplicationStr.map{$0^114}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case mainKeyFormat:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                RecoverPermission.shared.agent { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName, String(bytes: notiSinceMessage.map{platformDevice(system: $0)}, encoding: .utf8)!: auth, String(bytes: appFailureApplicationStr.map{$0^114}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case data_progressUrl:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        PossibleLocalizationBrightness.list(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        PossibleLocalizationBrightness.head(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case noti_backgroundMsg:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: app_fullMsg.reversed(), encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                ContainerNavigationDelegate.windowServer(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension ContainerNavigationDelegate {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func outsideFrom(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = ContainerNavigationDelegate()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        RetainedLoopTag.modern()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func windowServer(_ model: InformationProcessingSystemTransformable) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(constAlreadyId)" + (String(noti_statusMsg.suffix(4)) + "calPush")
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: noti_indexData.map{forwardBridge(by: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func center(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func camera() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func proxyStatus() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if ContainerNavigationDelegate.anvil() || ContainerNavigationDelegate.infoVpn() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func anvil() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(constPageMsg.prefix(6)) + dataVisiblePath.replacingOccurrences(of: "system", with: "xy"))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(kPhotoMessage) + userAreaName.replacingOccurrences(of: "empty", with: "x"))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(k_appearText.prefix(9)) + mainMirrorPath.replacingOccurrences(of: "environment", with: "y"))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func infoVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(k_dateTitle.capitalized + show_mapAppId.replacingOccurrences(of: "adjustment", with: "C") + "ED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(const_nowValue))) || keyStr.contains((mainScreenValue.replacingOccurrences(of: "space", with: "n"))) || keyStr.contains((mainArrowPath.replacingOccurrences(of: "fail", with: "s"))) || keyStr.contains((String(const_dismissName))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func file() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func clearDeadline(productId: String, source: Int = -1, payType: ShowType, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        EvaluationView.deviceCount()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        FullManager.shared.plainspoken(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            EvaluationView.timeGo()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    EvaluationView.local((String(userPageMsg.suffix(6)) + "After " + String(dataNowPath)) + "\"" + (String(dataNeverUrl) + app_trackName.replacingOccurrences(of: "view", with: "k")) + "\"" + (notiLocalSuccessMsg.replacingOccurrences(of: "active", with: " ") + String(mainApplicationFormatMaskMsg)))

                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.execute()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
